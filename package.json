{
  "name": "superblock-analytics-lambda",
  "version": "1.0.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "esbuild server/lambda/index.ts --platform=node --target=node20 --bundle --format=esm --outdir=dist/lambda --external:@aws-sdk/* --external:pg",
    "check": "tsc --noEmit",
    "test:notes": "tsx server/lambda/test-lambda.ts",
    "test:meetings": "tsx server/lambda/test-meetings.ts",
    "test:tables": "tsx server/lambda/test-all-tables.ts",
    "test:remaining": "tsx server/lambda/test-remaining.ts"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.1136.0",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/node": "^24.7.0",
    "@types/pg": "^8.23.1",
    "esbuild": "^0.25.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
